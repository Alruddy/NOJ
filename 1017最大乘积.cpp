#include<iostream>
#include<cstring>
using namespace std;
long long Cacul(char *resarray);
int mian()
{
    return 0;
}
long long Cacul(char *resarray)
{
    long long result;
    long long temp;
    for(int i=0;i<strlen(resarray);i++)
    {
        if()
    }
}
